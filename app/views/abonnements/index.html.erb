<p id="notice"><%= notice %></p>
<div id="legend">
  <legend class=""><h1 style="color: #8b8b8b;">Liste Cours</h1></legend>
</div>

    
      <div class="services" style="padding:0px!important;">
        <div class="container text-center">
          <div class="row">
            <% @cours.each do |abonnement| %>
            <% abonne = @abonnements.select{ |e|

              e.id_cours == abonnement.id

              } %>
            <div class="col-xs-4" >
               <%= image_tag "computer_science.jpg" %>
              <h3><%= link_to abonnement.nom, cour_path(abonnement.id)%></h3>
              <p> 
                  <%= abonnement.theme %>
              </p>
              <div class="row">
                <%= link_to 'Se désabonner', {:controller => "abonnements", :action => "destroy", :id => abonne[0].id}, method: :delete, data: { confirm: 'Are you sure?' }, :class => " btn btn-warning btn-md pull-right bttn" %>
                 
              </div>
            </div>
            <% end %>
          </div>
        </div>
      </div>