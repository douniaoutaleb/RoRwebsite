<%= form_for(@abonnement) do |f| %>
  <% if @abonnement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@abonnement.errors.count, "error") %> prohibited this abonnement from being saved:</h2>

      <ul>
      <% @abonnement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :id_user , class: "col-sm-2 control-label"%>
    <%= f.number_field :id_user, min:1%>
  </div>
  <div class="form-group">
    <%= f.label :id_cours , class: "col-sm-2 control-label"%>
    <%= f.number_field :id_cours , min:1 %>
  </div>
  <div class="actions" style="margin-left: 40%;margin-bottom: 2%;">
    <%= f.submit 'Sauvegarder', :class => "btn btn-default btn-outline "%>
    <%= link_to 'Retour', abonnements_path ,:class => "btn btn-default btn-outline "%>
  </div>
<% end %>
